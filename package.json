{
  "name": "hospitality-management-system",
  "version": "1.0.0",
  "scripts": {
    "build": "cd frontend && npm install && npm run build",
    "start": "cd frontend && npm ci && npm run build && cd ../backend && npm ci && npm start"
  },
  "engines": {
    "node": ">=18.0.0"
  }
}
